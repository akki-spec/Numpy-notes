"""used to split the array
    np.split(arr,index)
    for more then 2-dimensions
   vsplit -- vertical
   hsplit -- horizontal
   
   """
import numpy as np
arr = np.array([1,2,3,4,5,6])

print(np.split(arr,2))